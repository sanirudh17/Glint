/** Editor/annotation view stub — full implementation in a later task. */
export default function EditorView() {
  return (
    <div style={{ padding: "var(--s5)", color: "var(--text-dim)" }}>
      <span className="label">Editor</span>
    </div>
  );
}
