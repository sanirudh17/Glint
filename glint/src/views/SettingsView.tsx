/** Task 11 will build the real Settings view. */
export default function SettingsView() {
  return (
    <div style={{ padding: "var(--s5)", color: "var(--text-dim)" }}>
      <span className="label">Settings</span>
    </div>
  );
}
