/** Task 8 will build the real Home/capture view. */
export default function HomeView() {
  return (
    <div style={{ padding: "var(--s5)", color: "var(--text-dim)" }}>
      <span className="label">Home</span>
    </div>
  );
}
