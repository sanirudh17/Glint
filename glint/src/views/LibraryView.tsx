/** Task 9 will build the real Library/grid view. */
export default function LibraryView() {
  return (
    <div style={{ padding: "var(--s5)", color: "var(--text-dim)" }}>
      <span className="label">Library</span>
    </div>
  );
}
